@model Basket
    @if (User.Identity.IsAuthenticated)
    {
    <div class="ltn__utilize-menu-inner ltn__scrollbar">
        <div class="ltn__utilize-menu-head">
            <span class="ltn__utilize-menu-title">Cart</span>
            <button class="ltn__utilize-close">×</button>
        </div>
        <div class="mini-cart-product-area ltn__scrollbar">
            
                @if(Model.Products is not null)
                {
                     @foreach (var product in Model.Products)
                    {
                         <div class="mini-cart-item clearfix">
                            <div class="mini-cart-img">
                                  <a href="#"><img src="~/assets/uploads/images/@product.Images.Where(n=>n.IsMain ==true).FirstOrDefault().Url" alt="Image"></a>
                                <a data-id="@product.Id" class="mini-cart-item-delete delete-basket"><i class="icon-cancel"></i></a>
                            </div>
                            <div class="mini-cart-info">
                                <h6><a href="#">@product.Title</a></h6>
                               <span class="mini-cart-quantity">$@product.Price</span>
                            </div>
                         </div>
                    }
                }
        </div>
        <div class="mini-cart-footer">
            <div class="mini-cart-sub-total">
                    <h5>Subtotal: <span>$@Model.TotalPrice</span></h5>
            </div>
            <div class="btn-wrapper">
                <a href="cart.html" class="theme-btn-1 btn btn-effect-1">View Cart</a>
                <a href="cart.html" class="theme-btn-2 btn btn-effect-2">Checkout</a>
            </div>
            <p>Free Shipping on All Orders Over $100!</p>
        </div>

    </div>
    }else
    {
        <div class="h-100 w-100 d-flex align-items-center justify-content-center">
            <p>Please sign in for buy your favorite products!</p>
        </div>
    }
