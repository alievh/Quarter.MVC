@model List<Blog>

<div class="container">
    <div class="row">
       <div class="col-lg-12 grid-margin stretch-card">
          <div class="card">
            <div class="card-body">
              <h4 class="card-title">Blog - Table</h4>
              <div class="table-responsive">
                <div class="d-flex justify-content-between">
                    <a class="btn btn-secondary text-white" asp-controller="dashboard" asp-action="index">Go Back</a>
                    <div>
                        <a class="btn btn-success text-white" asp-controller="blog" asp-action="createcategory">Add BlogCategory</a>
                        <a class="btn btn-success text-white" asp-controller="blog" asp-action="create">Create</a>
                    </div>
                </div>
                <table class="table table-striped">
                  <thead>
                    <tr>
                      <th>
                        Image
                      </th>
                      <th>
                        Title
                      </th>
                      <th>
                        Setting
                      </th>
                    </tr>
                  </thead>
                  <tbody>
                     @foreach (var blog in Model)
                   {
                    <tr>
                        <td class="py-1">
                            @foreach (var image in blog.Images)
                           {
                                if (image.IsMain == true)
                                {
                                    <img src="~/assets/uploads/images/@image.Url" alt="image"/>
                                }
                           }
                        </td>
                        <td>
                          @blog.Title
                        </td>
                        <td>
                              <div class="d-flex">
                                  <a class="btn btn-primary text-white mx-1" asp-controller="blog" asp-action="detail" asp-route-id="@blog.Id">Detail</a>
                                  <a class="btn btn-warning text-white mx-1" asp-controller="blog" asp-action="update" asp-route-id="@blog.Id">Update</a>
                                  <a class="btn btn-danger text-white mx-1" asp-controller="blog" asp-action="delete" asp-route-id="@blog.Id">Delete</a>
                              </div>
                        </td>
                    </tr>
                   }
                  </tbody>
                </table>
              </div>
            </div>
          </div>
        </div>
    </div>
</div>